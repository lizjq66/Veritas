"""End-to-end loop test with mocked Hyperliquid SDK."""

# TODO: mock Info + Exchange, run one full cycle
#       observe → decide → declare → size → execute → monitor → exit → learn
#       assert assumption library is updated correctly
